import type { NextPage } from "next";
import Head from "next/head";

const Index: NextPage = ({}) => {
  return (
    <>
      <Head>
        <title>Arcade</title>
      </Head>
      view all
    </>
  );
};

export default Index;
